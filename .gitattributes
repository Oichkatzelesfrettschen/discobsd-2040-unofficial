# The root script runs on Windows too: keep LF.
/discobsd-console text eol=lf

# The instruction gate compares the generated compatibility copy byte for byte.
/AGENTS.md text eol=lf
/.claude/CLAUDE.md text eol=lf
