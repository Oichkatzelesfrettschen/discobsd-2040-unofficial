diff --git a/games/monop/Makefile b/games/monop/Makefile
index b3b08c1..4ad960c 100644
--- a/games/monop/Makefile
+++ b/games/monop/Makefile
@@ -26 +26 @@ initdeck:       initdeck.c deck.h
-		${HOST_CC} -o initdeck initdeck.c
+		${HOST_CC} ${WARNERR} -o initdeck initdeck.c
diff --git a/games/warp/Makefile b/games/warp/Makefile
index 74b0942..de483ad 100644
--- a/games/warp/Makefile
+++ b/games/warp/Makefile
@@ -41 +41 @@ sm:             sm.c
-		${HOST_CC} -O -Wall sm.c -o sm
+		${HOST_CC} ${WARNERR} -O -Wall sm.c -o sm
diff --git a/share/mk/sys.mk b/share/mk/sys.mk
index 7c8ea07..31f1400 100644
--- a/share/mk/sys.mk
+++ b/share/mk/sys.mk
@@ -23,0 +24,2 @@ TOOLBINDIR?=	${TOOLDIR}/bin
+include ${TOPSRC}/share/mk/warnings.mk
+
@@ -89 +91 @@ CC!=	if [ x"${MACHINE_ARCH}" = x"arm" ] ; then \
-		echo "${GCCPREFIX}-gcc -std=gnu17 -fno-common -mcpu=${MACHINE_CPU} -mabi=aapcs -mlittle-endian -mthumb -mfloat-abi=soft ${KERNEL_LAYOUT_CFLAGS} -nostdinc -I${TOPSRC}/include ${INCLUDES}" ; \
+		echo "${GCCPREFIX}-gcc ${WARNERR} -std=gnu17 -fno-common -mcpu=${MACHINE_CPU} -mabi=aapcs -mlittle-endian -mthumb -mfloat-abi=soft ${KERNEL_LAYOUT_CFLAGS} -nostdinc -I${TOPSRC}/include ${INCLUDES}" ; \
@@ -91 +93 @@ CC!=	if [ x"${MACHINE_ARCH}" = x"mips" ] ; then \
-		echo "${GCCPREFIX}-gcc -mips32r2 -EL -msoft-float -nostdinc -I${TOPSRC}/include ${INCLUDES}" ; \
+		echo "${GCCPREFIX}-gcc ${WARNERR} -mips32r2 -EL -msoft-float -nostdinc -I${TOPSRC}/include ${INCLUDES}" ; \
diff --git a/share/mk/warnings.mk b/share/mk/warnings.mk
new file mode 100644
index 0000000..b3d1db3
--- /dev/null
+++ b/share/mk/warnings.mk
@@ -0,0 +1,3 @@
+# Keep fatal diagnostics independent of CFLAGS: leaf makefiles often replace
+# optimization flags. Warning groups remain local to each source family.
+WARNERR=	-Werror
diff --git a/share/zoneinfo/Makefile b/share/zoneinfo/Makefile
index 55f1f5c..007568b 100644
--- a/share/zoneinfo/Makefile
+++ b/share/zoneinfo/Makefile
@@ -23 +23 @@ LINTFLAGS=	-phbaxc
-CC=		${HOST_CC}
+CC=		${HOST_CC} ${WARNERR}
diff --git a/sys/arch/rp2040/compile/PICO/Makefile b/sys/arch/rp2040/compile/PICO/Makefile
index 280f1e8..878e1d3 100644
--- a/sys/arch/rp2040/compile/PICO/Makefile
+++ b/sys/arch/rp2040/compile/PICO/Makefile
@@ -52 +52 @@ DEFS=		${INCLUDES} ${PARAM} -DKERNEL ${DEPFLAGS}
-CWARNFLAGS=	-Wall -Werror
+CWARNFLAGS=	-Wall -Wextra -Werror
diff --git a/sys/arch/rp2040/compile/PICO_UART/Makefile b/sys/arch/rp2040/compile/PICO_UART/Makefile
index 5f05b0c..b4c4c32 100644
--- a/sys/arch/rp2040/compile/PICO_UART/Makefile
+++ b/sys/arch/rp2040/compile/PICO_UART/Makefile
@@ -48 +48 @@ DEFS=		${INCLUDES} ${PARAM} -DKERNEL ${DEPFLAGS}
-CWARNFLAGS=	-Wall -Werror
+CWARNFLAGS=	-Wall -Wextra -Werror
diff --git a/sys/arch/rp2040/conf/Makefile.rp2040 b/sys/arch/rp2040/conf/Makefile.rp2040
index ca41ef8..a217834 100644
--- a/sys/arch/rp2040/conf/Makefile.rp2040
+++ b/sys/arch/rp2040/conf/Makefile.rp2040
@@ -27 +27 @@ DEFS=		${INCLUDES} ${PARAM} -DKERNEL ${DEPFLAGS}
-CWARNFLAGS=	-Wall -Werror
+CWARNFLAGS=	-Wall -Wextra -Werror
diff --git a/sys/arch/rp2040/dev/uart.c b/sys/arch/rp2040/dev/uart.c
index f5a4db2..ff96170 100644
--- a/sys/arch/rp2040/dev/uart.c
+++ b/sys/arch/rp2040/dev/uart.c
@@ -185,0 +186,2 @@ uartopen(dev_t dev, int flag, int mode)
+    (void)flag;
+    (void)mode;
@@ -233,0 +236,2 @@ uartclose(dev_t dev, int flag, int mode)
+    (void)flag;
+    (void)mode;
diff --git a/sys/arch/rp2040/dev/usb.c b/sys/arch/rp2040/dev/usb.c
index c0ecbd1..cc364ae 100644
--- a/sys/arch/rp2040/dev/usb.c
+++ b/sys/arch/rp2040/dev/usb.c
@@ -1033,0 +1034,2 @@ usbopen(dev_t dev, int flag, int mode)
+	(void)flag;
+	(void)mode;
@@ -1068,0 +1071,3 @@ usbclose(dev_t dev, int flag, int mode)
+	(void)dev;
+	(void)flag;
+	(void)mode;
@@ -1078,0 +1084 @@ usbread(dev_t dev, struct uio *uio, int flag)
+	(void)dev;
@@ -1084,0 +1091 @@ usbwrite(dev_t dev, struct uio *uio, int flag)
+	(void)dev;
@@ -1090,0 +1098 @@ usbioctl(dev_t dev, u_int cmd, caddr_t addr, int flag)
+	(void)dev;
@@ -1101,0 +1110 @@ usbselect(dev_t dev, int rw)
+	(void)dev;
@@ -1138,0 +1148 @@ usbputc(dev_t dev, char c)
+	(void)dev;
@@ -1192,0 +1203 @@ usbgetc(dev_t dev)
+	(void)dev;
@@ -1206,0 +1218 @@ usbprobe(struct conf_device *config)
+	(void)config;
diff --git a/sys/arch/rp2040/dhara/error.c b/sys/arch/rp2040/dhara/error.c
index 034cc83..c577afd 100644
--- a/sys/arch/rp2040/dhara/error.c
+++ b/sys/arch/rp2040/dhara/error.c
@@ -35 +35 @@ const char *dhara_strerror(dhara_error_t err)
-	if ((err >= 0) && (err < DHARA_E_MAX))
+	if ((unsigned int)err < DHARA_E_MAX)
diff --git a/sys/arch/rp2040/dhara/journal.c b/sys/arch/rp2040/dhara/journal.c
index 87743c3..11a0bbd 100644
--- a/sys/arch/rp2040/dhara/journal.c
+++ b/sys/arch/rp2040/dhara/journal.c
@@ -370,0 +371 @@ static int find_head(struct dhara_journal *j, dhara_page_t start,
+	(void)err;
diff --git a/sys/arch/rp2040/dhara/map.c b/sys/arch/rp2040/dhara/map.c
index bc1c4a8..b6fd63c 100644
--- a/sys/arch/rp2040/dhara/map.c
+++ b/sys/arch/rp2040/dhara/map.c
@@ -122 +122 @@ static int trace_path(struct dhara_map *m, dhara_sector_t target,
-	int depth = 0;
+	unsigned int depth = 0;
@@ -450 +450 @@ static int try_delete(struct dhara_map *m, dhara_sector_t s,
-	for (i = level + 1; i < DHARA_RADIX_DEPTH; i++)
+	for (i = level + 1; (unsigned int)i < DHARA_RADIX_DEPTH; i++)
