diff --git a/sys/kern/exec_subr.c b/sys/kern/exec_subr.c
index 7558aef..1c7db0e 100644
--- a/sys/kern/exec_subr.c
+++ b/sys/kern/exec_subr.c
@@ -88 +88 @@ exec_spool_write (struct exec_params *epp, const void *source, u_int len)
-        chunk = MIN (len, MAXBSIZE - epp->spool.fill);
+        chunk = MIN (len, (u_int)(MAXBSIZE - epp->spool.fill));
diff --git a/sys/kern/init_main.c b/sys/kern/init_main.c
index 6fe6a54..8e3169f 100644
--- a/sys/kern/init_main.c
+++ b/sys/kern/init_main.c
@@ -80 +80 @@ main(void)
-	for (i = 0; i < sizeof(u.u_rlimit)/sizeof(u.u_rlimit[0]); i++)
+	for (i = 0; i < (int)(sizeof(u.u_rlimit)/sizeof(u.u_rlimit[0])); i++)
diff --git a/sys/kern/kern_clock.c b/sys/kern/kern_clock.c
index 0bc0c53..5cbe8a8 100644
--- a/sys/kern/kern_clock.c
+++ b/sys/kern/kern_clock.c
@@ -52,0 +53 @@ gatherstats(pc, ps)
+    (void)pc;
diff --git a/sys/kern/kern_descrip.c b/sys/kern/kern_descrip.c
index a99d5d2..584de8b 100644
--- a/sys/kern/kern_descrip.c
+++ b/sys/kern/kern_descrip.c
@@ -429,0 +430,2 @@ fdopen(dev, mode, type)
+    (void)mode;
+    (void)type;
@@ -519 +521 @@ dupfdopen (indx, dfd, mode, error)
-        /* FALLTHROUGH */
+        return(error);
diff --git a/sys/kern/kern_fork.c b/sys/kern/kern_fork.c
index b8e4bb5..4e109f2 100644
--- a/sys/kern/kern_fork.c
+++ b/sys/kern/kern_fork.c
@@ -215 +215 @@ fork1 (isvfork)
-            if (p1->p_uid == u.u_uid)
+            if ((uid_t)p1->p_uid == u.u_uid)
@@ -218 +218 @@ fork1 (isvfork)
-            if (p1->p_uid == u.u_uid)
+            if ((uid_t)p1->p_uid == u.u_uid)
diff --git a/sys/kern/kern_prot.c b/sys/kern/kern_prot.c
index 8666885..6901c1b 100644
--- a/sys/kern/kern_prot.c
+++ b/sys/kern/kern_prot.c
@@ -83 +83 @@ getgroups()
-    if (uap->gidsetsize < gp - u.u_groups) {
+    if (uap->gidsetsize < (u_int)(gp - u.u_groups)) {
@@ -112 +112 @@ setpgrp()
-    if (p->p_uid != u.u_uid && u.u_uid && !inferior(p)) {
+    if ((uid_t)p->p_uid != u.u_uid && u.u_uid && !inferior(p)) {
diff --git a/sys/kern/kern_resource.c b/sys/kern/kern_resource.c
index 63bb3de..216b574 100644
--- a/sys/kern/kern_resource.c
+++ b/sys/kern/kern_resource.c
@@ -71 +71 @@ donice(p, n)
-        u.u_uid != p->p_uid && u.u_ruid != p->p_uid) {
+        u.u_uid != (uid_t)p->p_uid && u.u_ruid != (uid_t)p->p_uid) {
diff --git a/sys/kern/kern_sig.c b/sys/kern/kern_sig.c
index dcfc74c..d6185ac 100644
--- a/sys/kern/kern_sig.c
+++ b/sys/kern/kern_sig.c
@@ -41,2 +41,2 @@ cansignal (q, signum)
-        curp->p_uid == ruid ||      /* c effective = t real */
-        u.u_ruid == q->p_uid || /* c real = t effective */
+        (uid_t)curp->p_uid == ruid ||      /* c effective = t real */
+        u.u_ruid == (uid_t)q->p_uid || /* c real = t effective */
@@ -572 +572 @@ core()
-    if (USIZE + u.u_dsize + u.u_ssize >= u.u_rlimit[RLIMIT_CORE].rlim_cur)
+    if (USIZE + u.u_dsize + u.u_ssize >= (size_t)u.u_rlimit[RLIMIT_CORE].rlim_cur)
diff --git a/sys/kern/kern_synch.c b/sys/kern/kern_synch.c
index 8858093..c4de212 100644
--- a/sys/kern/kern_synch.c
+++ b/sys/kern/kern_synch.c
@@ -31,0 +32 @@ schedcpu (caddr_t arg)
+    (void)arg;
@@ -348,0 +350 @@ setrun (p)
+        return;
diff --git a/sys/kern/kern_sysctl.c b/sys/kern/kern_sysctl.c
index 153e810..3948925 100644
--- a/sys/kern/kern_sysctl.c
+++ b/sys/kern/kern_sysctl.c
@@ -279,0 +280 @@ hw_sysctl(int *name, u_int namelen, void *oldp, size_t *oldlenp,
+	(void)newlen;
@@ -365,0 +367 @@ vm_sysctl(int *name, u_int namelen, void *oldp, size_t *oldlenp,
+	(void)newlen;
@@ -486 +488,2 @@ sysctl_string(void *oldp, size_t *oldlenp, void *newp, size_t newlen,
-	int len, error = 0;
+	size_t len;
+	int error = 0;
@@ -491 +494 @@ sysctl_string(void *oldp, size_t *oldlenp, void *newp, size_t newlen,
-	if (newp && newlen >= maxlen)
+	if (newp && newlen >= (size_t)maxlen)
@@ -510 +513,2 @@ sysctl_rdstring(void *oldp, size_t *oldlenp, void *newp, const char *str)
-	int len, error = 0;
+	size_t len;
+	int error = 0;
@@ -533 +537 @@ sysctl_struct(void *oldp, size_t *oldlenp, void *newp, size_t newlen,
-	if (oldp && *oldlenp < len)
+	if (oldp && *oldlenp < (size_t)len)
@@ -535 +539 @@ sysctl_struct(void *oldp, size_t *oldlenp, void *newp, size_t newlen,
-	if (newp && newlen > len)
+	if (newp && newlen > (size_t)len)
@@ -554 +558 @@ sysctl_rdstruct(void *oldp, size_t *oldlenp, void *newp, void *sp, int len)
-	if (oldp && *oldlenp < len)
+	if (oldp && *oldlenp < (size_t)len)
@@ -570 +574,2 @@ sysctl_file(char *where, size_t *sizep)
-	int buflen, error;
+	size_t buflen;
+	int error;
@@ -688,2 +693,2 @@ sysctl_doproc(int *name, u_int namelen, char *where, size_t *sizep)
-	int needed = 0;
-	int buflen = where != NULL ? *sizep : 0;
+	size_t needed = 0;
+	size_t buflen = where != NULL ? *sizep : 0;
@@ -732 +737 @@ again:
-			if (p->p_uid != (uid_t)name[1])
+			if ((uid_t)p->p_uid != (uid_t)name[1])
diff --git a/sys/kern/subr_prf.c b/sys/kern/subr_prf.c
index 902d467..3f560f6 100644
--- a/sys/kern/subr_prf.c
+++ b/sys/kern/subr_prf.c
@@ -212,0 +213 @@ reswitch:
+            /* FALLTHROUGH */
diff --git a/sys/kern/subr_rmap.c b/sys/kern/subr_rmap.c
index 8faab4c..3f4a556 100644
--- a/sys/kern/subr_rmap.c
+++ b/sys/kern/subr_rmap.c
@@ -313 +313,2 @@ malloc3_contiguous_next (struct map *mp, size_t d_size, size_t s_size,
-            if (bp->m_addr > (size_t)-1 - bp->m_size)
+            end = (size_t)bp->m_addr + (size_t)bp->m_size;
+            if (end < (size_t)bp->m_addr)
@@ -315 +315,0 @@ malloc3_contiguous_next (struct map *mp, size_t d_size, size_t s_size,
-            end = bp->m_addr + bp->m_size;
diff --git a/sys/kern/sys_generic.c b/sys/kern/sys_generic.c
index 197aa49..bae57a6 100644
--- a/sys/kern/sys_generic.c
+++ b/sys/kern/sys_generic.c
@@ -39 +39 @@ rwuio (uio)
-    for (iov = uio->uio_iov, i = 0; i < uio->uio_iovcnt; i++, iov++)
+    for (iov = uio->uio_iov, i = 0; i < (u_int)uio->uio_iovcnt; i++, iov++)
@@ -43 +43 @@ rwuio (uio)
-    if (uio->uio_resid != total) {      /* check wraparound */
+    if (uio->uio_resid != (u_long)total) {      /* check wraparound */
@@ -431,0 +432,2 @@ seltrue(dev, flag)
+    (void)dev;
+    (void)flag;
@@ -461,0 +464,2 @@ sorw(fp, uio)
+    (void)fp;
+    (void)uio;
@@ -476,0 +481,3 @@ soctl(fp, com, data)
+    (void)com;
+    (void)data;
+    (void)fp;
@@ -488,0 +496,2 @@ sosel(fp, flag)
+    (void)flag;
+    (void)fp;
@@ -499,0 +509 @@ socls(fp)
+    (void)fp;
@@ -531,0 +542 @@ nostrategy (bp)
+    (void)bp;
