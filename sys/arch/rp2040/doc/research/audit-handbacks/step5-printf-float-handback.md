# RP2040 printf floating conversion source and linkage audit

Authority: the parent verified clean `main` at `ccfa7882e75414e2b28baa84c21b74f90b55d1a9` in `<repository root>` before the audit, then observed HEAD `386b8f7239e02b5f6901583cbc69d648c91138d2`. The parent's exact `git diff --name-status ccfa7882e75414e2b28baa84c21b74f90b55d1a9 386b8f7239e02b5f6901583cbc69d648c91138d2` result contains only `M games/bubble/bubble.c`, outside every admitted source path. The agent re-read all 351 admitted source files after that notification and compared bytes with its in-memory source snapshot: all 351 match. Source claims therefore bind to the recorded content hashes and identical admitted source at both named commits. The audit reads bounded source, two supplied JSONs, and 15 retained ELF/a.out pairs; it writes only this handback and classifies exactly the 30 JSON rows whose `explicit_root_names` contains `__doprnt_cvt`.

| Tool | Resolved path | Version | Surface |
|---|---|---|---|
| Python | `/usr/local/bin/python3` | 3.14.7, GCC 16.2.1 | Read-only parsing, byte accounting, ELF/a.out comparison; resolved once with `PYTHON_INTERPRETER=$(command -v python3)` |
| Git | `/usr/bin/git` | 2.55.0 | `git grep -Ilz` tracked source enumeration |
| ripgrep | `/usr/bin/rg` | 15.2.0 | Tool identification |
| jq | `/usr/bin/jq` | 1.8.2 | JSON schema/row inspection |
| ARM nm/objdump | `/usr/bin/arm-none-eabi-nm`, `/usr/bin/arm-none-eabi-objdump` | GNU Binutils 2.47.20260726 | Defined symbols and existing ELF disassembly |
| sha256sum | `/usr/bin/sha256sum` | GNU coreutils 9.11 | Input hashes |

`required` means a source-reachable, supported floating output path needs the converter. `removable` means the admitted program output formats are bounded to integer, pointer, string, character, or literal output; removal remains a source/link-policy candidate. `unknown` names a missing format or library closure. These classifications assume defined program behavior and the checked-in target configuration. They establish neither runtime correctness nor relinked byte recovery nor root-filesystem recovery.

Every flag originates in the row's directory Makefile (`PRINTF_FLOAT=yes`), with shared inheritance for BASIC and UUC targets. `share/mk/sys.mk:109-115` converts the declaration into `-Wl,-u,__doprnt_cvt` for RP2040. `lib/libc/stdio/doprnt.c:60-62,358-363,385,415-420` declares the weak converter and dispatches `e/E/f/F/g/G`; the weak-absent branch prints `?` for finite numbers after consuming the double. The source comment's approximate 10 KiB figure supplies context, rather than a measured opportunity.

Linked-evidence notation: `J` is the matching supplied all-text JSON row and its explicit root; `E` means retained `<program>.elf`, whose entire ELF `.text` bytes equal the corresponding a.out bytes beginning at offset 32. All 15 inspected pairs pass that comparison. `S1328` means nm measures `__doprnt_cvt` as `0x530` = 1,328 symbol bytes; the number counts existing symbol extent only, excludes support/rodata/alignment, and supplies no recovery guarantee. `U` means an opportunity quantity remains unmeasured. Source citations below are repository-relative; abbreviated filenames resolve inside the program directory.

| Program | Flag origin | Format evidence | Linked evidence | Classification | Raw byte opportunity | Falsifier / remaining gate |
|---|---|---|---|---|---|---|
| usr.bin/ar/ar | Makefile:2 | contents.c:80-104 uses integers/strings; `%e` at 84 belongs to strftime. archive.h:109-113 closes HDR1/2/3 used by archive.c:296-306. misc.c:155-158 closes error wrapper. | J root 0x20001ab0; ELF absent | removable | U | A compiled printf-format origin outside the enumerated constants/macros |
| usr.bin/awk/awk | Makefile:2 | tran.c:33 sets OFMT=`%.6g`; 198-200 sprintf numeric values. run.c:347-388 builds user formats and dispatches f/e/g with double arguments. | J root 0x200059d8; source format/getsval absent from unreachable candidates; ELF absent | required | 0 admitted | Show the numeric-to-string and language printf paths excluded by the target build |
| usr.bin/basic/basic | Makefile:2,13-17 | basic.c:476-478,780,893-1011,1247 uses fixed integer/string date/error/line formats. Interpreter output supplies characters; its date formatter selects constant sprintf formats. | J root 0x20004f0c; ELF absent | removable | U | A reachable BASIC output operation feeding an unbounded printf format |
| usr.bin/basic/renumber | Makefile:2,19-23 | renumber.c:44-49,64-68,90,120 uses literals and `%u` only. Shared flag inherited from BASIC directory. | J root 0x200003fc; ELF absent | removable | U | A floating or dynamically supplied output format in renumber's compiled closure |
| usr.bin/bc/bc | Makefile:2 | bc.y:818-872 uses `%d`, `%lx`, `%s`; emits dc language text. Decimal arithmetic/output in a separate executed program is outside bc's a.out. | J root 0x2000134c; ELF absent | removable | U | A bc printf-family call receiving a floating conversion rather than emitted dc text |
| usr.bin/diskspeed/diskspeed | Makefile:2 | diskspeed.c:167-170,187-190 prints `%u.%03u` seconds and integer throughput. Other diagnostics use fixed `%d/%s`. | J root 0x2000048c; ELF absent | removable | U | A target-specific floating throughput printf path |
| usr.bin/egrep/egrep | Makefile:2 | egrep.y:594-601,655-656 prints names/counts with `%s/%ld`; diagnostics use literals/`%s`. Grammar `%left` is syntax. | J root 0x2000113c; ELF absent | removable | U | A compiled printf format beyond the enumerated literal format set |
| usr.bin/forth/forth | Makefile:2 | func.c:805-810 Ffprint prints `%g`; dictionary entry `f.` at 1924 makes the function selectable. forth.c:780 prints floating instruction operands. | J retains Ffprint as a stored-pointer root; converter root 0x20003e70 | required | 0 admitted | Show the `f.` dictionary entry and floating operand display removed from the target |
| usr.bin/lex/lex | Makefile:2 | header.c:70 and parser.y:283 use escaped `%%e`. sub1.c:346,368,403,422 passes literal `%e` help text as a `%s` argument; error/diagnostic wrapper at 19-38 formats once. All admitted wrapper formats are non-floating. | J root 0x20006ae8; ELF absent | removable | U | A reachable warning/error caller passing float conversion in its format argument |
| usr.bin/od/od | Makefile:2 | od.c:199-204 selects dble/flt; descriptor callbacks at 81-82; d_put/f_put call sprintf `%21.14e` / `%14.7e` at 460/477. | J retains d_put and f_put as stored-pointer roots; converter 0x20000b64 | required | 0 admitted | Show both floating descriptors unavailable in the built option dispatcher |
| usr.bin/picoc/picoc | Makefile:2; CFLAGS:9 | `-DNO_FP` disables supported float dispatch at cstdlib/stdio.c:220-224,278-287. Unguarded StdioFprintfFP/PrintFP and floating test files are dead evidence. Dynamic OneFormatBuf construction at 211-274 can preserve unsupported float letters before an integer/pointer suffix. | E; J marks StdioFprintfFP (74 B) and PrintFP (24 B) unreachable; ELF has zero direct calls to either; StdioFprintfWord/Pointer remain linked | unknown | U; S1328 retained | Prove every accepted dynamic format reaching integer/pointer wrappers excludes floating conversion, including unsupported-letter sequences; supported NO_FP behavior alone leaves that proof open |
| usr.bin/re/re | Makefile:9 | r.main.c:441-463 and r.edit.c:1331-1355 printf calls are DEBUG-only; r.gettc.c:139-142 is TEST-only. All use non-floating formats. r.termcap.c:181-245 tgoto implements its own integer terminal grammar. | E; nm defines converter at 0x20004de8, S1328; nm defines zero printf-family or _doprnt symbols; objdump finds zero direct printf-family calls | removable | S1328 | A new compiled formatter caller or enabled float source path; DEBUG/TEST enabling alone still supplies only non-floating formats |
| usr.bin/retroforth/retroforth | Makefile:2 | retro.c:129 terminal escape; 281/293 image errors; 624-662 integer optional statistics; 689 `%s`. VM emits characters; tools/convert.c belongs to another target. | J root 0x20002614; ELF absent | removable | U | A VM primitive forwarding an arbitrary printf format instead of character output |
| usr.bin/scm/scm | Makefile:2 | scm.c:506-516 putatom TREAL prints `%#g`; putexpr dispatch at 539/548 and REPL evaluation output at 1400-1405 reaches atom printing. | J root 0x20004154; putatom absent from unreachable candidates; ELF absent | required | 0 admitted | Show real objects or putexpr-to-putatom output excluded from the target |
| usr.bin/tclsh/tclsh | Makefile:2,10 | tclsh.c:48,62,75,95,200-210 uses integer/string formats; Tcl command implementation is linked by `-ltcl` and lies outside admitted source. | J root 0x20009c64; ELF absent | unknown | U | Admit libtcl format-command/numeric rendering closure and establish its accepted floating output formats |
| usr.bin/uucp/uucico | uucp/Makefile:9,119-123 | condevs.c:431-446 genbrk DEBUG `%f`; uucp.h:269 expands DEBUG to fprintf. conn.c:685/771 calls genbrk; conn.c:1081 also has configured chat sprintf. | E; main 0x200006aa calls conn; sendthem 0x20004e7c/0x20004fee calls genbrk; genbrk 0x20006174-0x2000618c converts int to float, divides, promotes to double, calls fprintf | required | 0 admitted | Show built Debug>=4 break path and all other floating output unavailable; hardware execution remains outside audit |
| usr.bin/uucp/uuclean | uucp/Makefile:9,83-87 | uuclean.c:75-87,183-185,216 integer/string diagnostics/mail; linked shared library format origins remain fixed non-floating. | E; S1328 at 0x200018b0; genbrk/dochat absent; a.out floating-format string census empty | removable | S1328 | A linked dynamic printf-format origin beyond the bounded shared-library macros |
| usr.bin/uucp/uucp | uucp/Makefile:9,59-63 | uucp.c:254-259,286,339,361,384 uses modes, names, and command strings. xuucico executes another binary rather than linking genbrk. | E; S1328 at 0x200025b8; genbrk/dochat absent; floating-format string census empty | removable | S1328 | A linked float format or a dynamic format passed as the format argument |
| usr.bin/uucp/uudecode | uucp/Makefile:9,53-57 | uudecode.c:41,48,66,72,92,112 literals/`%s`; input decoding and sscanf provide a separate input surface. | E; S1328 at 0x200003f4; only main calls printf-family; floating-format string census empty | removable | S1328 | A floating output path added to main or the decoder |
| usr.bin/uucp/uuencode | uucp/Makefile:9,47-51 | uuencode.c:36,43,47 uses literals and `begin %o %s`; encoding emits characters. | E; S1328 at 0x200003f4; only main calls printf-family; floating-format string census empty | removable | S1328 | A floating output format beyond mode/name framing |
| usr.bin/uucp/uulog | uucp/Makefile:9,77-81 | uulog.c:36,49,56,67,71 uses names and shell command strings. Log content is data for executed cat/egrep. | E; S1328 at 0x20000bf0; genbrk/dochat absent; floating-format string census empty | removable | S1328 | A printf format derived from log content inside uulog |
| usr.bin/uucp/uuname | uucp/Makefile:9,89-93 | uuname.c:47-63 prints names via `%s` and literal usage/error framing. | E; S1328 at 0x20000a10; genbrk/dochat absent; floating-format string census empty | removable | S1328 | A reachable format source beyond name strings and fixed shared diagnostics |
| usr.bin/uucp/uupoll | uucp/Makefile:9,95-99 | uupoll.c:31-64 prints names and builds lock/poll paths with `%s/%c/%.*s`; remote execution helper is a process boundary. | E; S1328 at 0x20001ad8; genbrk/dochat absent; floating-format string census empty | removable | S1328 | A linked dynamic printf-format origin outside fixed poll/shared-library formats |
| usr.bin/uucp/uuq | uucp/Makefile:9,113-117 | uuq.c:139 prints `%3.1f` minutes; 154 prints `%5.1f` job time in main's queue listing. | E; main 0x20000000 size 0x43c calls printf; both exact float strings retained; converter 0x200013c8 | required | 0 admitted | Show queue listing paths containing both conversions excluded from the built main |
| usr.bin/uucp/uusend | uucp/Makefile:9,107-111 | uusend.c:172-174 constructs names/mode commands; 293 counts bytes with `%ld`; other diagnostics `%s`. | E; S1328 at 0x20000580; only main calls printf-family; floating-format string census empty | removable | S1328 | A format string generated from destination/content rather than passed through `%s` |
| usr.bin/uucp/uusnap | uucp/Makefile:9,101-105 | uusnap.c:72-124 prints string/count/time fields with `%s/%d/%ld`; 149 diagnostic `%s`. | E; S1328 at 0x2000079c; main/scandir printf callers; floating-format string census empty | removable | S1328 | A floating time renderer added to the snapshot output |
| usr.bin/uucp/uux | uucp/Makefile:9,65-69 | uux.c:25-26,157-164,262,403 command records use `%c/%s`; 462 integer exit code. | E; S1328 at 0x20002a44; genbrk/dochat absent; floating-format string census empty | removable | S1328 | A linked floating diagnostic or an unbounded format argument |
| usr.bin/uucp/uuxqt | uucp/Makefile:9,71-75 | uuxqt.c:309,328,339,351 formats integer exit status and command/name strings; 634 `%s` status. | E; S1328 at 0x200030c0; genbrk/dochat absent; floating-format string census empty | removable | S1328 | A linked dynamic printf format outside the fixed execution/status diagnostics |
| usr.bin/vmstat/vmstat | Makefile:2 | vmstat.c:341-347 prints CPU percentages `%3.0f`; 400/404 averages `%.2f`; 417 disk rate `%4.0f`. | J root 0x20000c18; stats/doforkst absent from unreachable candidates; ELF absent | required | 0 admitted | Show all normal/rate/fork floating display paths excluded by configuration |
| usr.sbin/iostat/iostat | Makefile:2 | iostat.c:202 prints terminal rates `%4.0f%5.0f`; 222/225 disk rates; 240 CPU `%3.0f`. | J root 0x200007b8; stats absent from unreachable candidates; ELF absent | required | 0 admitted | Show all floating rate/percentage output excluded from target |

Totals: 30 classified = 8 required + 20 removable + 2 unknown. UUC contributes 2 required + 11 removable; its shared flag declaration therefore over-selects 11 source-format closures. Twelve removable rows retain inspected ELFs, giving 12 x 1,328 = 15,936 existing converter symbol bytes as a bounded raw candidate surface. Eight removable rows have unmeasured symbol extents. Support routines may remain co-retained or independently required. The supplied libgcc-filtered JSON's pre-existing candidate sets root __doprnt_cvt and therefore cannot quantify removing that root; adding their ceilings would double-count unrelated mechanisms.

Gaps: (1) picoc's NO_FP dynamic format grammar requires proof or an explicitly scoped semantics rule; a user string such as `%fd` can reach the integer formatter after preserving `f`, with an incompatible variadic argument. The audit identifies that risk statically and executes nothing. (2) libtcl sources and tclsh ELF lie outside the admitted/retained evidence. (3) Fifteen programs lack retained ELF files, so their exact compiled source-to-call correspondence rests on source configuration plus supplied JSON evidence. (4) Byte recovery needs separate root-removal graph/archive analysis and authorized relinking. (5) Admitted extra directories, test sources, and unused UUC modules are inventory inputs only; their floating hits do not establish behavior of any selected a.out. (6) Standard-library fixed format origins visible only as retained calls (for example asctime/tztab) were bounded with linked string evidence for inspected binaries; runtime correctness remains unmeasured.

The source input set is exactly nonempty tracked regular files matching `:(glob)<directory>/**/<pattern>` for the directories below and patterns `*.c`, `*.h`, `Makefile`, `*.mk`, `*.y`, `*.l`, plus `share/mk/sys.mk` and `lib/libc/stdio/doprnt.c`. Python stores decoded contents once for subsequent scans; all 351 source files decode without replacement characters. Byte accounting counts each unique regular input once, as confirmed by the parent.

| Admitted source directory | Files | Bytes |
|---|---:|---:|
| bin/csh | 30 | 245426 |
| usr.bin/ar | 14 | 60050 |
| usr.bin/awk | 12 | 63922 |
| usr.bin/basic | 3 | 49470 |
| usr.bin/bc | 2 | 14067 |
| usr.bin/diskspeed | 2 | 5716 |
| usr.bin/egrep | 2 | 16040 |
| usr.bin/forth | 5 | 67490 |
| usr.bin/lex | 7 | 56750 |
| usr.bin/od | 2 | 21564 |
| usr.bin/pforth | 36 | 552918 |
| usr.bin/picoc | 92 | 459618 |
| usr.bin/re | 13 | 148362 |
| usr.bin/retroforth | 10 | 24207 |
| usr.bin/scm | 4 | 63312 |
| usr.bin/tclsh | 2 | 5029 |
| usr.bin/unixbench | 21 | 159423 |
| usr.bin/uucp | 83 | 354048 |
| usr.bin/virus | 5 | 122734 |
| usr.bin/vmstat | 2 | 12000 |
| usr.sbin/iostat | 2 | 6772 |
| share/mk/sys.mk; libc doprnt.c | 2 | 16023 |

Unique read total: sources 2,524,941 + supplied JSONs 1,962,528 + 15 ELF files 1,575,272 + 15 a.out files 459,658 = **6,522,399 bytes**, below 8,388,608. The exact binary set is picoc, re, and all 13 UUC rows, each with the matching `.elf` suffix. ELF `.text` equality establishes the measured text byte surface; complete file identity and runtime behavior remain separate claims.

SHA-256: all-text JSON `621db6c7507c74e4602027e128567a3b9828a619e09e6a785d6b2f8ad4453999`; libgcc-filtered JSON `2c4080c1b5bf06f748065453e0a5bdefa3401030022bc9c5e5ac1ef8d1d2689d`. Source manifest hash `16b5f8383da7951a21083d210e896609b5127a1b1b17314ff9a570e4777b6830` hashes UTF-8 lines `relative_path<TAB>byte_size<TAB>sha256(content)<LF>` sorted by relative path. Critical source hashes: sys.mk `47bd930efddd84f675e2e1cce6a7c8e5deb80aa6190fb8494ded83c301a5ca20`; doprnt.c `d034bfe750857d623490a122beeae224030ba52de41c04413b3bfbe1fe7e95f8`; re/Makefile `1abb539c1b1a06f2ed35e4bd283cdf9b817ddc5cdfeb131a0ea36edfcc82dadd`; uucp/Makefile `28d2918dcee7b5d441f5d99944c05d5e59de6528c15539e21f04eea5311b86d3`.

Exact command forms used: `jq 'type, (if type=="object" then keys else length end)' <each-json>`; `jq '.programs[0]' <each-json>`; `/usr/bin/rg --version`; `jq --version`; `sha256sum <all-text-json> <libgcc-filtered-json>`; `git --version`; `sha256sum --version`; `arm-none-eabi-nm --version`. Full JSON paths reside in `<audit scratch directory>/` with basenames `aout-reachability-all-text-bounded.json` and `aout-reachability-all-libgcc-filtered.json`.

Source enumeration executes Python `patterns=[f':(glob){directory}/**/{pattern}' for directory in dirs for pattern in ('*.c','*.h','Makefile','*.mk','*.y','*.l')]`, then `subprocess.run(['git','grep','-Ilz','-e','','--',*patterns],cwd=root,stdout=subprocess.PIPE,check=True)`. Here `dirs` is exactly the directory table excluding its final two-file row; `root=Path('<repository root>')`. The two explicit files are appended, stat sizes checked against 8 MiB including JSONs, and `read_text(errors='replace')` loads the source map. Selected rows use `rows=[row for row in json.loads(all_text.read_text())['programs'] if '__doprnt_cvt' in row['explicit_root_names']]`.

For each existing selected `<program>.elf`, the audit executes `subprocess.run(['/usr/bin/arm-none-eabi-nm','-S','--defined-only',str(path)],stdout=subprocess.PIPE,check=True,text=True)` and `subprocess.run(['/usr/bin/arm-none-eabi-objdump','-d',str(path)],stdout=subprocess.PIPE,check=True,text=True)`. Python identifies caller blocks through `r'[0-9a-f]+ <([^>]+)>:'` and direct `bl` targets `(v?fprintf|v?sprintf|v?snprintf|printf|vprintf)`. Source-call inspection tokenizes C strings, characters, comments and balanced argument parentheses, then inspects printf-family format arguments, wrapper callers, macros, build flags and dispatch tables; lexical hits alone supply leads.

ELF comparison uses `struct.unpack_from('<16sHHIIIIIHHHHHH',elf)` for ELF32 section-table metadata and `<10I` for each section header. The section-name string table resolves `.text`; its `[sh_offset:sh_offset+sh_size]` bytes compare with `aout[32:32+sh_size]`. The a.out header uses `<8I`; inspected magic is 263. The linked string census searches `rb'[\x20-\x7e\n\r\t]{4,}\x00'` and `rb'%(?:[-+ #0]*)(?:[0-9]+|\*)?(?:\.(?:[0-9]+|\*))?[hlL]?[eEfFgG]'` after removing escaped `%%`. The census supports the source format closure; its empty result alone cannot prove arbitrary dynamically constructed formats safe.
