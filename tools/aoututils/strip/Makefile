#
# strip - discard symbols from object files
#

PROG=	strip
OBJS=	strip.o

# -Wall -Wextra raises 1 distinct sites in this directory, so the fatal set
# stays at the groups CFLAGS names until they are repaired.
WARNLEVEL=	legacy
include	../Makefile.inc
